export enum ETransmission {
  auto = "auto",
  manual = "manual",
  mixed = "mixed",
}
export enum ETradeType {
  rent = "rent",
  sale = "sale",
}
export enum EFuelType {
  gasoline = "gasoline",
  diesel = "diesel",
  electric = "electric",
}
